[package]
name = "desktop"
version = "0.1.0"
edition = "2024"

[features]
default = ["desktop"]
desktop = ["dioxus/desktop", "drydrop/desktop"]

[dependencies]
drydrop = { path = "../../crates/drydrop", default-features = false }

dioxus = { workspace = true }
dioxus-components = { path = "../../crates/dioxus-components" }
