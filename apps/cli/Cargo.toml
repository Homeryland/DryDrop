[package]
name = "cli"
version = "0.1.0"
edition = "2024"

[dependencies]
drydrop = { path = "../../crates/drydrop", default-features = false, features = ["cli"] }

usage = { workspace = true }

snafu = { workspace = true }
